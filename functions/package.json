{
  "name": "functions",
  "version": "1.0.0",
  "description": "Lambda function for csv processor",
  "main": "CsvProcessor.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prebuild": "rm -rf dist && npm i",
    "postbuild": "cp -r node_modules dist/"
  },
  "author": "Pankaj",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "8.10.3",
    "@types/pg": "^7.14.11",
    "aws-sdk": "2.894.0",
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "pg": "^8.6.0"
  }
}
